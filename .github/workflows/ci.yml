name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Set up Python
        run: uv python install

      - name: Install dependencies
        run: uv sync --group dev

      - name: Lint with ruff
        run: uv run ruff check .

      - name: Check 150-line/file rule
        run: bash scripts/check_line_count.sh

      - name: Run tests with coverage
        run: uv run pytest --cov=sine_denoiser --cov-report=term-missing --cov-fail-under=85
